import React from "react";
import { ProjectTable } from "@modules/project";
import { Layout } from "@modules/layout";

const Projects = () => {
  return (
    <Layout>
      <ProjectTable />
    </Layout>
  );
};
export default Projects;
