import React from "react";
import { Layout } from "@modules/layout";
import { CreateTaskModal } from "@modules/CreateTask";

const CreateTask = () => {
  return (
    <Layout>
      <CreateTaskModal />
    </Layout>
  );
};

export default CreateTask;
